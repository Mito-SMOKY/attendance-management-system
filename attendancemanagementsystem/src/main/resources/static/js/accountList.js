/**
 * CSVファイル出力処理
 */
function exportCsv() {
    // ファイル出力処理
    if (confirm("CSVファイルを出力しますか？")) {
        // 実装例：サーバーのエンドポイントへリダイレクトする場合
        // location.href = '/admin/export-accounts';
        
        alert("ファイル出力処理を実行します");
    }
}